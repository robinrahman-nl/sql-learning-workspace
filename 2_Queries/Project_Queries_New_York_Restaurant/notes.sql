--

SELECT * FROM nomnom;

-- ============================================================
-- PROJECT: New York Restaurants
-- COURSE: Learn SQL / Queries
-- ------------------------------------------------------------




-- PROJECT CONTEXT
-- This project focuses on querying an existing dataset (`nomnom`)
-- to extract insights using SELECT, WHERE, DISTINCT, ORDER BY,
-- LIMIT, LIKE, CASE, and logical operators (AND / OR).
--
-- The table schema is predefined by Codec accompanied and is not
-- created or modified in this project.
--
-- ------------------------------------------------------------
-- LEARNING GOALS
-- - Explore an unfamiliar table using SELECT *
-- - Identify unique values with DISTINCT
-- - Filter rows using WHERE, AND, OR
-- - Use pattern matching with LIKE
-- - Handle NULL / empty values
-- - Rank results with ORDER BY + LIMIT
-- - Apply conditional logic using CASE
--
-- ------------------------------------------------------------
-- TASK CHECKLIST
--
-- 1) Inspect table structure and column names
-- 2) List distinct neighborhoods
-- 3) List distinct cuisine types
-- 4) Filter restaurants by cuisine (Chinese)
-- 5) Filter by review score (>= 4)
≈
-- 6) Combine conditions (Italian AND $$$)
-- 7) Search by partial name using LIKE
-- 8) Filter by multiple neighborhoods (OR)
-- 9) Identify rows with missing health grades
-- 10) Create a Top 10 ranking based on reviews
-- 11) Use CASE to map numeric reviews to text ratings
--
-- ------------------------------------------------------------
-- NOTES FOR FUTURE ME
-- - This project is READ-ONLY (queries only)
-- - No schema changes are expected
-- - Focus is on query logic, not data mutation
-- - Results depend entirely on the provided dataset
-- ============================================================

--


SELECT * FROM nomnom;

--